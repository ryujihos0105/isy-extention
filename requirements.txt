# ── PyTorch (CUDA 버전은 환경에 맞게 선택) ───────────────────────────────────
# CUDA 11.8:  pip install torch==2.7.1+cu118 torchvision==0.22.1+cu118 --index-url https://download.pytorch.org/whl/cu118
# CUDA 12.1:  pip install torch==2.7.1+cu121 torchvision==0.22.1+cu121 --index-url https://download.pytorch.org/whl/cu121
# CPU only:   pip install torch==2.7.1 torchvision==0.22.1
# torch==2.7.1      ← pip install -r 로 설치하지 마세요. 위 주석의 CUDA 버전별 명령으로 먼저 설치하세요.
# torchvision==0.22.1

# ── 서버 ─────────────────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn==0.30.6
pydantic==2.7.4

# ── 이미지 처리 ───────────────────────────────────────────────────────────────
Pillow==10.4.0
numpy==2.0.2
opencv-python==4.10.0.84

# ── 모델 ─────────────────────────────────────────────────────────────────────
timm==1.0.9
insightface==0.7.3
onnxruntime==1.19.2
open-clip-torch>=2.20.0   # versionv12 (TripleFusion: RGB+FFT+CLIP) 브랜치용

# ── 네트워크 ──────────────────────────────────────────────────────────────────
requests==2.32.3
yt-dlp==2025.4.30
python-multipart==0.0.20
# 백신/프록시 TLS 검사 환경에서 open_clip 가중치(huggingface.co) 다운로드 SSL 검증용.
# Windows 인증서 저장소를 사용하게 한다. Python 버전에 맞는 것 하나만 설치:
#   Python >= 3.10:  truststore>=0.9.0        (server.py 가 자동 주입)
#   Python <  3.10:  pip-system-certs         (.pth 로 requests 자동 패치, 코드 불필요)
# 설치 시 SSL 에 막히면:  pip install <pkg> --trusted-host pypi.org --trusted-host files.pythonhosted.org
truststore>=0.9.0 ; python_version >= "3.10"
pip-system-certs ; python_version <  "3.10"
