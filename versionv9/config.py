# 이 파일은 데모 실행에 필요한 모든 설정값을 담당합니다 (v9 LateFusion FFT 방식 B)

# ── 경로 설정 ─────────────────────────────────────────────
MODEL_PATH = "weights/best.pt"             # v9 fusion_fftB 학습 가중치 파일 경로
IMAGE_PATH = "../demo/samples/test.jpg"    # 추론할 이미지 기본 경로 (demo/ 폴더 참조)

# ── 모델/이미지 설정 ──────────────────────────────────────
IMG_SIZE    = 224            # 모델 입력 이미지 크기
FACE_MARGIN = 0.25           # 얼굴 크롭 시 여백 비율

# ── ImageNet 정규화 통계 ──────────────────────────────────
IMAGENET_MEAN = [0.485, 0.456, 0.406]
IMAGENET_STD  = [0.229, 0.224, 0.225]

# ── FFT 방식 B 하이퍼파라미터 ─────────────────────────────
# 학습 시 (노트북 SimCap_v9_FFT_B_1.ipynb CFG)와 동일하게 유지해야 함
FFT_CUTOFF    = 0.1     # 시그모이드 고주파 마스크 컷오프 (낮을수록 더 많은 고주파 통과)
FFT_STEEPNESS = 10.0    # 마스크 경계 선명도
